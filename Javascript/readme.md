# js opplæring

## Diverse oppgaver

### **Oppgave 1 – Variabler og if-else**

Lag en variabel `alder`.

* Hvis alderen er **18 eller mer**, skriv ut `"Du er myndig"`
* Hvis ikke, skriv ut `"Du er ikke myndig"`

👉 Test med minst **to ulike verdier**.

---

### **Oppgave 2 – Logiske operatorer (&&, ||)**

Lag to variabler:

```js
let brukernavn = "admin";
let passord = "1234";
```

Sjekk:

* Hvis brukernavn er `"admin"` **OG** passord er `"1234"`, skriv `"Innlogging OK"`
* Ellers skriv `"Feil brukernavn eller passord"`

---

### **Oppgave 3 – if / else if**

Lag en variabel `poeng` (0–100).

Skriv ut karakter basert på poeng:

* 90–100 → `"Karakter 6"`
* 75–89 → `"Karakter 5"`
* 60–74 → `"Karakter 4"`
* Under 60 → `"Ikke bestått"`

---

### **Oppgave 4 – for-løkke**

Bruk en `for`-løkke som:

* Skriver ut **alle tall fra 1 til 10**
* Hvis tallet er **partall**, skriv `"X er et partall"`
* Hvis ikke, skriv `"X er et oddetall"`

Eksempel:

```
2 er et partall
3 er et oddetall
```

---

### **Oppgave 5 – while-løkke og logikk**

Lag en variabel `teller` som starter på `5`.

* Bruk en `while`-løkke
* Skriv ut tallet så lenge telleren er **større enn 0**
* Trekk fra 1 hver runde

Til slutt skal det skrives:

```
Ferdig!
```

